<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <!-- style name="Base.Theme.MyQuizApplication" parent="Theme.MaterialComponents.DayNight.DarkActionBar" is used if you want an action bar -->
    <style name="Base.Theme.MyQuizApplication" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/design_default_color_primary</item>
        <item name="colorPrimaryVariant">@color/design_default_color_primary</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/white</item>
        <item name="colorSecondaryVariant">@color/design_default_color_secondary</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor"  >?attr/colorPrimaryVariant</item>
        <!--This is how you remove the top status bar when the app is launched-->
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="Theme.MyQuizApplication" parent="Base.Theme.MyQuizApplication" />
</resources>